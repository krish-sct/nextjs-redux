import React from "react";
import List from "./List";

export const metadata = {
  title: {
    default: "Podcast",
  },
};

const Podcast = async () => {
  return <List />;
};

export default Podcast;
