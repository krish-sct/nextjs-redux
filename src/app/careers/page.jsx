import React from "react";
import List from "./List";

export const metadata = {
  title: {
    absolute: "Careers",
  },
};
const Careers = async () => {
  return <List />;
};

export default Careers;
